# Vampire Survivor Game

This is a simple survival game built with [Pygame](https://www.pygame.org/).

## Requirements
- Python 3
- Pygame (install via `pip install pygame`)

## How to Run
```bash
python main.py
```

Use the arrow keys to move the player square. Avoid touching the red enemies for as long as possible. The game displays the survival time on the screen.
